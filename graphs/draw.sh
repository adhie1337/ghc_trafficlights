echo 'Drawing a.dot' &&
    dot -Tpng a.dot -o a.png &&
    echo 'Drawing b.dot' &&
    dot -Tpng b.dot -o b.png &&
    echo 'Drawing c.dot' &&
    dot -Tpng c.dot -o c.png &&
    echo 'Drawing d.dot' &&
    dot -Tpng d.dot -o d.png &&
    echo 'Drawing e.dot' &&
    dot -Tpng e.dot -o e.png &&
    echo 'Drawing f.dot' &&
    dot -Tpng f.dot -o f.png
